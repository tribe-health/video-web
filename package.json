{
  "name": "video-web",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "build": "npm build",
    "serve": "http-server . -a 127.0.0.1 -p 8888",
    "start": "npm run serve",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "http-server": "^0.12.3",
    "test-ipfs-example": "^3.0.0"
  },
  "dependencies": {
    "ipfs": "^0.55.1"
  }
}
